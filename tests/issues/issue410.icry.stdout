Loading module Cryptol
{n, a} (Logic a) => n == min (1 + n) n
{n, a} (Logic a) => Logic a
{n, a} (Logic a) => Logic a
{n, a} (Logic a) => True
(s
 where
   s x =
       (bs
        where
          bs = [~b | b <- [~x] # bs
                   | _ <- bs]
        )
 ) : {n, a} (Logic a) => a -> [n]a
{n, a} n == n
(bs
 where
   bs = [b | b <- bs]
 ) : {n, a} [n]a
1 == 1
[b | b <- [True]] : [1]
