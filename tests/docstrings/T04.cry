module T03 where

/**
 * Only a subset of REPL commands work in docstrings
 *
 * ```repl
 * :load AFile.cry
 * ```
 *
 * Stuff not marked repl is ignored
 * ```
 * garbage
 * ```
 */
a : Bit
a = True
